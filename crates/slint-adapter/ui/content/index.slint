import { BuiltinDashboard } from "../builtin/dashboard.slint";
import { TabData } from "../content/globals.slint";
import { Theme } from "theme.slint";
import {
    Navigation,
} from "../features/navigation/globals/tabs-navigation.slint";
export component ContentContainer inherits Rectangle {
    clip: true;

    private property <TabData> active_tab: Navigation.tabs[Navigation.active_tab_index];

    if (root.active_tab.id == 0): BuiltinDashboard { }
    if (root.active_tab.id != 0): Rectangle { }
}
