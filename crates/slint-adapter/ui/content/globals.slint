
export enum PageStatus {
    inactive,
    loading,
    ready,
    error
}

export global MainBodyState {
    in-out property <bool> is-loading: true;
    in-out property <PageStatus> status: PageStatus.loading;
    in-out property <string> error-msg: "Unknown error occurred";
}
