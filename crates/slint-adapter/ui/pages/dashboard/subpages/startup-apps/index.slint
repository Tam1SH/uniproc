import { PlaceholderPage } from "../shared/placeholder-page.slint";

export global StartupAppsPageSpec {
    out property <string> layout: "with-sidebar";
}

export component StartupAppsPage inherits Rectangle {
    PlaceholderPage {
        title: "Startup apps";
    }
}
