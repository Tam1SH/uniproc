import { PlaceholderPage } from "../shared/placeholder-page.slint";

export global StatisticsPageSpec {
    out property <string> layout: "with-sidebar";
}

export component StatisticsPage inherits Rectangle {
    PlaceholderPage {
        title: "Statistics";
    }
}
