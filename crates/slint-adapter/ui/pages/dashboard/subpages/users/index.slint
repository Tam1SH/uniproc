import { PlaceholderPage } from "../shared/placeholder-page.slint";

export global UsersPageSpec {
    out property <string> layout: "with-sidebar";
}

export component UsersPage inherits Rectangle {
    PlaceholderPage {
        title: "Users";
    }
}
